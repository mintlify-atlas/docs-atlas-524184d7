---
title: "Exercises API: Browse the Blackterz Exercise Catalog"
sidebarTitle: "Exercises"
description: "Retrieve the full exercise catalog with muscle groups, images, and GIFs. One authenticated GET returns every exercise sorted alphabetically by name."
---

The Exercises API exposes the entire Blackterz exercise catalog in a single authenticated request. Each exercise object includes its name, category, static image, optional animated GIF URL, and a comma-separated string of associated muscle group names resolved via a SQL `GROUP_CONCAT` join. The catalog covers barbell, dumbbell, bodyweight, machine, and cable movements.

---

## GET /api/ejercicios

Returns all exercises in the catalog, ordered alphabetically by name. Muscle groups are resolved by a SQL `LEFT JOIN` against the `ejercicios_grupos_musculares` and `grupos_musculares` tables, collapsed into a single `musculos` string per row using `GROUP_CONCAT`.

**Auth required:** Yes — `Authorization: Bearer <token>`

### Request

No query parameters or request body. Authentication is handled entirely by the `Authorization` header.

```bash
curl -X GET https://api.blackterzgym.com/api/ejercicios \
  -H "Authorization: Bearer <your_token>"
```

### Response

<ResponseField name="status" type="string" required>
  Always `"ok"` on success.
</ResponseField>

<ResponseField name="data" type="array" required>
  Array of exercise objects. Ordered alphabetically by `nombre`.

  <Expandable title="Exercise object fields">
    <ResponseField name="id" type="integer">
      Unique exercise identifier.
    </ResponseField>

    <ResponseField name="nombre" type="string">
      Exercise name in Spanish (e.g., `"Press de banca"`, `"Sentadilla"`).
    </ResponseField>

    <ResponseField name="descripcion" type="string">
      Short description of the exercise and its primary equipment type.
    </ResponseField>

    <ResponseField name="categoria" type="string">
      Exercise category (e.g., `"fuerza"`).
    </ResponseField>

    <ResponseField name="imagen_url" type="string | null">
      Filename of the static preview image, e.g. `"bench-press.avif"`. Serve from `/images/<imagen_url>`.
    </ResponseField>

    <ResponseField name="gif_url" type="string | null">
      URL to an animated demonstration GIF. Returns `null` when no GIF has been configured for this exercise.
    </ResponseField>

    <ResponseField name="musculos" type="string | null">
      Comma-separated list of muscle group names involved in this exercise, ordered alphabetically. Built via SQL `GROUP_CONCAT`. Example: `"Glúteos, Isquiotibiales, Trampas"`. Returns `null` if no muscle groups are mapped.
    </ResponseField>
  </Expandable>
</ResponseField>

### Sample response

```json
{
  "status": "ok",
  "data": [
    {
      "id": 32,
      "nombre": "Abdominales",
      "descripcion": "Ejercicio de peso corporal para Core.",
      "categoria": "fuerza",
      "imagen_url": "crunches.avif",
      "gif_url": null,
      "musculos": "Core"
    },
    {
      "id": 33,
      "nombre": "Abdominales sentado",
      "descripcion": "Ejercicio de peso corporal para Core.",
      "categoria": "fuerza",
      "imagen_url": "sit-ups.avif",
      "gif_url": null,
      "musculos": "Core, Flexores Cadera"
    },
    {
      "id": 61,
      "nombre": "Aductores en máquina",
      "descripcion": "Ejercicio de máquina para Aductores.",
      "categoria": "fuerza",
      "imagen_url": "hip-adduction.avif",
      "gif_url": null,
      "musculos": "Aductores"
    }
  ]
}
```

### Error responses

<Accordion title="500 Internal Server Error">
  Returned when the database query fails.

  ```json
  {
    "status": "error",
    "message": "Error interno del servidor"
  }
  ```
</Accordion>

<Accordion title="401 Unauthorized">
  Returned when no token is provided or the token is invalid/expired. Handled by the `verificarToken` middleware before reaching this controller.

  ```json
  {
    "status": "error",
    "message": "Token no válido o expirado"
  }
  ```
</Accordion>

### Full curl example

<CodeGroup>
```bash cURL
curl -X GET https://api.blackterzgym.com/api/ejercicios \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
```

```javascript JavaScript (fetch)
const res = await fetch('/api/ejercicios', {
  headers: {
    Authorization: `Bearer ${token}`,
  },
});
const { status, data } = await res.json();
```

```python Python (requests)
import requests

resp = requests.get(
    'https://api.blackterzgym.com/api/ejercicios',
    headers={'Authorization': f'Bearer {token}'}
)
exercises = resp.json()['data']
```
</CodeGroup>
